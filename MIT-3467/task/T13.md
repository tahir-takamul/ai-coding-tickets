# T13 — Delete `.github/workflows/trigger-sonar-analysis.yaml`

**Status**: pending
**Repo**: this repo (`/Users/mohd.tahir/DevWorkspace/sonar-azure/mithril/.github/workflows/`)
**Blocks**: —
**Blocked by**: T11

## Reason

The legacy GHA workflow triggers a Jenkins job that scans against the old external Sonar (`dsocodescan.cbuae.gov`), publishing to the legacy `mithril` project. After this rollout, that flow is obsolete — the new Sonar lives in-cluster and is consumed by Jenkins via the `cicd-sonarqube-token` credential, not by an old webhook.

See FINDING §F21.

## Input

- T11 verified the new path works.
- Sweep for callers before deletion:

```
grep -rn "trigger-sonar-analysis" mithril/
grep -rn "mithril-sonar-trigger-token" mithril/
grep -rn "dsocodescan.cbuae.gov" mithril/
```

The first should only hit the workflow itself + maybe READMEs / docs (update those in the same commit). The second is the legacy webhook token — any non-workflow hit means another caller; investigate before deleting. The third should be empty after T12 deletes the root properties file (which references the legacy host) and any doc updates.

## Output

- Delete `.github/workflows/trigger-sonar-analysis.yaml`.
- Update any README / doc that references this workflow or the legacy `dsocodescan.cbuae.gov` host. Specifically check `README.MD` (line ~257 referenced by the previous task plan; verify line number current).

## Acceptance criteria

- File is absent.
- `gh workflow list` no longer shows the legacy workflow (or shows it as disabled if GH retains state for a few hours).
- `grep -rn "trigger-sonar-analysis" mithril/` returns zero results.
- `grep -rn "dsocodescan.cbuae.gov" mithril/` returns zero results.

## Notes

- The Jenkins JCasC for the **old** `sonar-analysis` job (in the on-prem JCasC or wherever the AWS Jenkins lives) can be removed independently once no callers remain. That is **cross-repo** and **out of scope** for this task — coordinate with whoever owns that Jenkins config.
- DO NOT delete this file before T11 verifies the new pipeline works. There's no rollback path for the legacy flow once deleted (well — `git revert`, but the JCasC token may already be reused/rotated by then).
- This task can be done in the same PR as T12.
- After T13 lands, the `mithril/` repo no longer references SonarQube anywhere except CLAUDE.md / READMEs / and this `task/` directory. Per-module integration (follow-up) re-introduces references via per-module properties files and updated CI workflows.
