# T05 — Helm values for SonarQube

**Status**: pending
**Repo**: `[SIBLING] /Users/mohd.tahir/DevWorkspace/azure_jenkins/mithril/iac/eng-infra/cicd/k8s/sonarqube/`
**Blocks**: T06, T08
**Blocked by**: —

## Reason

Encodes every deployment-shape decision: image tag, bundled Postgres, persistence on `managed-csi-premium`, workload-identity pod label, SAML wiring, monitoring passcode, etc. The Ansible playbook (T06) consumes this file via `helm upgrade --install`.

See FINDING §F05 (chart + bundled Postgres), §F12 (SAML cert via Secret), §F13 (workload identity label/annotation), §F23 (chart-version-dependent keys).

## Input

- Read `[SIBLING] cicd/k8s/jenkins/values.yaml` lines 622-634 — workload-identity SA + storage-class pattern to mirror.
- Read `[SIBLING] cicd/k8s/jenkins/values.yaml` lines 226-240 — SAML JCasC block; mirror its **shape** (claim mappings) in Sonar's `sonar.auth.saml.*` properties.
- Run `helm show values sonarqube/sonarqube --version <pinned>` before committing; verify keys for `account.adminPasswordSecretName`, `monitoringPasscodeSecretName`, `postgresql.auth.existingSecret`, `sonarSecretProperties`.

## Output

### `[SIBLING] cicd/k8s/sonarqube/values.yaml`

```yaml
image:
  repository: sonarqube
  tag: "community"          # F03: floating tag, monthly bump cadence
  pullPolicy: IfNotPresent

community:
  enabled: true

deploymentType: StatefulSet
replicaCount: 1

resources:
  requests: { cpu: 500m, memory: 2Gi }
  limits:   { cpu: 2000m, memory: 4Gi }

persistence:
  enabled: true
  storageClass: managed-csi-premium
  size: 10Gi
  accessMode: ReadWriteOnce

extensions:
  persistence:
    enabled: true
    storageClass: managed-csi-premium
    size: 1Gi

sysctlInitContainer:
  enabled: true            # chart's privileged init sets vm.max_map_count=524288, fs.file-max=131072
initContainers:
  image: busybox:1.36

postgresql:
  enabled: true            # bundled Postgres — F05
  auth:
    username: sonarUser
    database: sonarDB
    existingSecret: sonarqube-credentials
    secretKeys:
      adminPasswordKey: db-password
      userPasswordKey:  db-password
  primary:
    persistence:
      enabled: true
      storageClass: managed-csi-premium
      size: 10Gi

account:
  adminPasswordSecretName: sonarqube-credentials
  # NOTE: chart key shape is version-dependent (F23). Verify before T08.

monitoringPasscodeSecretName: sonarqube-credentials
monitoringPasscodeSecretKey:  monitoring-passcode

serviceAccount:
  create: true
  name: sonarqube
  # azure.workload.identity/client-id annotation patched in post-install by playbook (F13)

podLabels:
  azure.workload.identity/use: "true"

service:
  type: ClusterIP
  externalPort: 9000

ingress:
  enabled: false           # Cloudflare tunnel terminates externally (T09)

# SonarQube SAML — mirrors Jenkins JCasC shape (cicd/k8s/jenkins/values.yaml:226-240)
sonarProperties:
  sonar.core.serverBaseURL:        https://sonarqube.takamul.cc
  sonar.auth.saml.enabled:         true
  sonar.auth.saml.providerName:    "Azure Entra ID"
  sonar.auth.saml.applicationId:   "api://__SAML_APP_ID__/sonarqube-sso"
  sonar.auth.saml.providerId:      "https://sts.windows.net/af9e95b7-e99f-4ec1-9b02-429ffdc27b4e/"
  sonar.auth.saml.loginUrl:        "https://login.microsoftonline.com/af9e95b7-e99f-4ec1-9b02-429ffdc27b4e/saml2"
  sonar.auth.saml.user.login:      "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress"
  sonar.auth.saml.user.name:       "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/name"
  sonar.auth.saml.user.email:      "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress"
  sonar.auth.saml.group.name:      "http://schemas.microsoft.com/ws/2008/06/identity/claims/groups"

# SAML cert is too long to inline; loaded from secret at runtime via secret.properties (F12)
sonarSecretProperties: sonarqube-credentials
# The Secret 'sonarqube-credentials' (created by the playbook, T06) must contain a key
# `secret.properties` whose body is:
#   sonar.auth.saml.certificate.secured=<single-line PEM body, no BEGIN/END>
```

The string `__SAML_APP_ID__` is templated by the playbook from `sonarqube_saml_application_id` (group_var, T04) at render time.

## Acceptance criteria

- `helm template sonarqube sonarqube/sonarqube --version {{ sonarqube_helm_version }} -f values.yaml` renders without error.
- The rendered StatefulSet pod template carries label `azure.workload.identity/use: "true"`.
- The rendered Service has `type: ClusterIP` and port `9000`.
- The rendered chart references Secret `sonarqube-credentials` for admin/db/monitoring/saml-cert.

## Notes

- `community.enabled: true` is the chart's switch for "this is the Community Build edition" — required when using the `community` image tag.
- `replicaCount: 1` is **mandatory** — Community Build does not support HA (F04). Don't bump.
- Plugin install left empty: per the user-confirmed decision, **no** `mc1arke/community-branch-plugin`. Don't add `plugins.install: [...]`.
- Storage classes mirror Jenkins's pattern (`managed-csi-premium`).
- If the chart version chosen for T04 has migrated `account.adminPasswordSecretName` to a different key (F23), update this file accordingly before T08.
