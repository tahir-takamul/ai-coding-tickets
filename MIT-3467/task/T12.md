# T12 — Delete root `sonar-project.properties`

**Status**: pending
**Repo**: this repo (`/Users/mohd.tahir/DevWorkspace/sonar-azure/mithril/`)
**Blocks**: —
**Blocked by**: T11

## Reason

The legacy root `sonar-project.properties` was authored for the external Sonar at `dsocodescan.cbuae.gov` with `sonar.projectKey=mithril`. After T08 the new in-cluster SonarQube is the canonical server; the legacy file is stale (wrong project key, points at the old server, lists 12 dirs vs the actual 16 — see FINDING §F22).

This rollout's scope is server stand-up only. Per-module scan integration (per-module `sonar-project.properties`) is a follow-up. In the gap, **no** root `sonar-project.properties` is the right state — leaving the legacy file would mis-point any caller that runs `sonar-scanner` from the repo root. See FINDING §F20.

## Input

- T11 verified server is up and the smoke pipeline works.
- Sweep for callers of the legacy file before deletion:

```
grep -rn "sonar-project.properties" mithril/
grep -rn "sonar.projectKey=mithril$\|sonar.projectKey=mithril\b" mithril/
grep -rn "dsocodescan.cbuae.gov" mithril/
```

The first should mostly hit the file itself + maybe `.gitleaks.toml` / docs. The second should not return anything outside the file. The third may return `.github/workflows/trigger-sonar-analysis.yaml` — that file is deleted in T13.

## Output

- Delete `sonar-project.properties` at the repo root.

## Acceptance criteria

- File is absent.
- `grep -rn "^sonar.projectKey=mithril$" mithril/` returns no results.
- No CI job in this repo fails after deletion. The legacy GHA workflow that referenced the legacy server is also deleted in T13.

## Notes

- DO NOT replace with a stub or a placeholder. The per-module follow-up creates `backend/`, `portal/`, `wallet/` `sonar-project.properties` files as the proper replacement.
- The legacy `mithril` project on `dsocodescan.cbuae.gov` stays as-is for history. Communicate the cutover to anyone watching that dashboard if relevant.
- This task can be done in the same PR as T13.
