# T04 — Ansible group_vars updates

**Status**: pending
**Repo**: `[SIBLING] /Users/mohd.tahir/DevWorkspace/azure_jenkins/mithril/iac/eng-infra/cicd/ansible/inventory/group_vars/`
**Blocks**: T06, T08
**Blocked by**: T01 (workload identity client_id), T02 (SAML application_id)

## Reason

Playbook T06 references `sonarqube_*` Jinja vars and the shared `cicd_secret_names` dict. These need to exist with sane defaults so the playbook stays thin and re-runnable.

## Input

- Read `[SIBLING] cicd/ansible/inventory/group_vars/all.yml` lines 22-30 — current `cicd_secret_names` dict (Jenkins keys). Append SonarQube keys.
- Read `[SIBLING] cicd/ansible/inventory/group_vars/cloud.yml` lines 10-11 — confirms `key_vault_name: mithril-shared-kv`. Append SonarQube vars to this file.
- T01 output: `terraform output sonarqube_workload_identity_client_id` — paste this value into `cloud.yml`.
- T02 output: `terraform output sonarqube_sso_application_id` — paste this value into `cloud.yml`.

## Output

### Append to `[SIBLING] cicd/ansible/inventory/group_vars/all.yml`

Add the chart version and extend `cicd_secret_names` with the six SonarQube keys (preserve existing Jenkins keys exactly):

```yaml
sonarqube_helm_version: "10.8.0"   # verify exact patch via
                                   # `helm search repo sonarqube/sonarqube --versions | grep 10.8`
                                   # before T08; chart key shape may differ between minor versions (F23)

cicd_secret_names:
  # ... existing Jenkins keys preserved ...
  sonarqube_admin_password:           "cicd-sonarqube-admin-password"
  sonarqube_current_admin_password:   "cicd-sonarqube-current-admin-password"
  sonarqube_db_password:              "cicd-sonarqube-db-password"
  sonarqube_monitoring_passcode:      "cicd-sonarqube-monitoring-passcode"
  sonarqube_saml_cert:                "cicd-sonarqube-saml-cert"
  sonarqube_token:                    "cicd-sonarqube-token"
```

### Append to `[SIBLING] cicd/ansible/inventory/group_vars/cloud.yml`

```yaml
sonarqube_url: "https://sonarqube.takamul.cc/"
sonarqube_workload_identity_client_id: "<paste from `terraform output sonarqube_workload_identity_client_id`>"

sonarqube_saml_tenant_id:       "af9e95b7-e99f-4ec1-9b02-429ffdc27b4e"
sonarqube_saml_application_id:  "<paste from `terraform output sonarqube_sso_application_id`>"
sonarqube_saml_admin_group_id:  "0309fec6-57e6-4446-bf4f-af084823d03a"  # Takamul DevOps Admin
```

## Acceptance criteria

- `ansible-inventory --list -i inventory/<env>/hosts` (with the `cloud` group selected) shows the new vars resolved.
- `cicd_secret_names` round-trips through the playbook's secret-fetch loop without errors when run with `--check` (no live KV writes).
- `sonarqube_helm_version` is pinned to a real tag — confirm via `helm search repo sonarqube/sonarqube --versions | head -20` before T08.

## Notes

- Adding the SonarQube keys to the shared `cicd_secret_names` dict means re-running `01-jenkins.yml` will also fetch SonarQube secrets into Jenkins's secret-bag. **This is intentional** — Jenkins needs `cicd-sonarqube-token` later (T10) so its pipelines can scan. The dict is shared by design.
- Do NOT split into two separate secret dicts. The pasted plan calls out the overlap as harmless.
- `sonarqube_workload_identity_client_id` is committed in plain text — this is a public identifier (not a credential), same as Jenkins's client_id is committed today.
