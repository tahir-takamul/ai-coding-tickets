# T04 — Port `ios-build` placeholder module to mobile-v2 + wire into bootstrap-orchestrator

**Status**: pending (orchestrator wired with stub; macOS hardware blocks real implementation)
**Repo**: `takamulai/mithril` — `iac/cicd-onprem/pipelines/mobile-v2/modules/ios-build.Jenkinsfile`
**Blocks**: — (iOS upload not yet wired anywhere)
**Blocked by**: macOS build agent provisioning (external to this ticket)

## Reason

The bootstrap-orchestrator's `OS` parameter accepts `iOS` and `Both`. To keep
the parameter shape consistent with legacy and to leave a clear
"swap-in" point when macOS hardware is provisioned, mobile-v2 carries an
`ios-build` module that mirrors the legacy placeholder shape — a
proper pipeline with commented-out Xcode stages.

## Input

- Read `iac/cicd-onprem/pipelines/mobile/modules/ios-build.Jenkinsfile`
  — the source placeholder to port.
- Confirm with the platform team whether macOS hardware ETA is within
  this ticket's window. If not, this task stays parked.

## Output

### `iac/cicd-onprem/pipelines/mobile-v2/modules/ios-build.Jenkinsfile`

Port the legacy placeholder verbatim with header refresh:

- Header references `mobile-v2/` and this T04 spec; same warning that
  macOS hardware is required.
- Same `BRANCH` / `ENVIRONMENT` / `COMMIT_SHA` parameter triplet as
  android-build, so the orchestrator wires both with the same args.
- Commented-out Xcode stages (`xcodebuild archive`, `xcodebuild
  -exportArchive`, signing, etc.) ready to uncomment.
- Current behaviour: print a message and mark the build `UNSTABLE`
  (matches legacy — does not fail the parent stage).

### Bootstrap orchestrator wiring (already done; this spec documents contract)

`mobile-v2/orchestrators/bootstrap-orchestrator.Jenkinsfile`:

- `Build iOS` stage gated by
  `when { expression { params.OS in ['iOS','Both'] } }`.
- Marks `env.IOS_BUILD_STATUS = result.result` in the post-summary so
  the stage's `UNSTABLE` outcome is visible.

## Acceptance criteria

- File exists at `mobile-v2/modules/ios-build.Jenkinsfile`.
- Running with `OS=iOS` invokes the module, the module prints its "on
  hold pending macOS hardware" message and exits `UNSTABLE`.
- The bootstrap pipeline's overall result is `SUCCESS` when iOS is the only
  stage running and the other stages would have skipped — i.e. iOS
  `UNSTABLE` does not cascade to `FAILURE`. (Confirmed via the legacy
  pipeline's behaviour; preserved here by `propagate: false` + result
  capture.)

## Notes

- When macOS hardware lands, the upgrade path is: (a) swap `agent` to
  the macOS label, (b) uncomment the Xcode stages, (c) wire an
  `Upload iOS Artifact` stage in the bootstrap-orchestrator (mirrors the
  Android upload).
- The `OS=Both` path doesn't run iOS effectively today, but it doesn't
  fail the build either — the UNSTABLE result is contained.
