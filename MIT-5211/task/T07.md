# T07 — Cloudflare Tunnel sibling PR (eng-infra)

**Status**: **DONE (authored, uncommitted in eng-infra)** — 2026-05-15
**Repo**: `takamulai/eng-infra` — `cloudflare/terraform.tfvars`
**Blocks**: T08
**Blocked by**: — (G6 = file the PR; this task authors the diff for that PR)

## Reason

APISIX is `ClusterIP`-only. External traffic reaches it through the
existing silmarils Cloudflare Zero Trust Tunnel (`silmarils-dev`
tunnel — name is historical; scopes the whole silmarils AKS cluster).
Exposing the QA APISIX requires one new `silmarils_ingress_rules`
entry pointing the public hostname at the in-cluster Service. No new
tunnel, no Access policy, no DNS hand-editing (the CNAME is auto-created
by the existing `cloudflare_record.silmarils_tunnel_ingress` resource).

## Input

- `eng-infra/cloudflare/main.tf:450` — current silmarils tunnel
  definition.
- `eng-infra/cloudflare/terraform.tfvars` — current
  `silmarils_ingress_rules` list.
- PLAN.md §Cloudflare Tunnel ingress.

## Output

### Edit `cloudflare/terraform.tfvars`

Append to `silmarils_ingress_rules`:

```hcl
{
  hostname = "silmarils-qa-apisix.takamul.cc"
  service  = "https://apisix.apisix-silmarils.svc.cluster.local:19888"
},
```

No other changes. `origin_no_tls_verify` is already `true` at the
tunnel level (accommodates the internal-CA server cert).

### PR description (suggested)

> ### Summary
>
> Adds one ingress rule to the existing silmarils Cloudflare Zero
> Trust Tunnel for the new QA APISIX gateway (MIT-5211).
> `silmarils-qa-apisix.takamul.cc` → `https://apisix.apisix-silmarils.svc.cluster.local:19888`.
> No new tunnel, no Access policy — mTLS is terminated by APISIX
> itself via `X-Forwarded-Client-Cert` (matches the local-dev contract).
> `origin_no_tls_verify` already `true` at tunnel level.
>
> ### Test plan
> - `terraform plan` shows a single `cloudflare_zero_trust_tunnel_*`
>   ingress-rule addition, nothing else.
> - After apply: `dig silmarils-qa-apisix.takamul.cc` returns the
>   tunnel CNAME (`<tunnel-id>.cfargotunnel.com`).
> - Hitting the hostname before MIT-5211 deploys yields 502 from
>   cloudflared (no origin yet); after MIT-5211 T08 deploys, 200.

## Acceptance criteria

- `terraform plan` diff scope = exactly one ingress-rule addition.
- Reviewer from eng-infra approves and merges.
- Post-apply `dig` returns the tunnel CNAME.
- Cloudflare dashboard lists the new ingress rule under the silmarils
  tunnel.

## Notes

- Can be authored in parallel with T03–T06; sequencing constraint is
  only "merged before T08 runs the through-tunnel smoke".
- A future `silmarils-dev-apisix.takamul.cc` entry will be added when
  dev comes online — same shape, no plan changes needed.
- If a deviation surfaces (e.g. the tunnel is dedicated per-app and
  this rollout needs a new tunnel), open a FINDING and re-plan — do
  not silently change the tunnel scope.
- The PR is **out-of-band** (different repo) from the main MIT-5211
  silmarils PR. Both must merge for QA smoke to pass.

## Execution log (2026-05-15)

- Authored inline by Claude (no agent — single 13-line edit).
- File touched: `/Users/mohd.tahir/DevWorkspace/apisix/eng-infra/cloudflare/terraform.tfvars`
  (added 13 lines after line 190, before the `]` closing
  `silmarils_ingress_rules`).
- **Deviation from existing silmarils QA pattern (deliberate, worth
  flagging at PR-review time):** all other silmarils-qa entries route
  `service = "https://ingress-nginx-controller.ingress-nginx.svc.cluster.local:443"`
  (front-NGINX, Host-header-routed). APISIX entry routes
  **direct-to-Service** at
  `https://apisix.apisix-silmarils.svc.cluster.local:19888`. This
  bypasses ingress-nginx because APISIX itself terminates and routes —
  layering nginx in front would be a wasted hop and would force the
  NGINX Ingress to terminate TLS twice. Documented inline as a comment
  block in `terraform.tfvars`.
- `options_preflight_bypass` is **not** set (this is server-to-server
  traffic, no browser CORS preflight involved).
- `terraform plan` not run yet — that's part of the PR-review cycle in
  `takamulai/eng-infra`, not this ticket.
- Status: **eng-infra-side change is uncommitted**. To be opened as a
  sibling PR against `takamulai/eng-infra` (likely concurrent with the
  MIT-5211 silmarils PR).
