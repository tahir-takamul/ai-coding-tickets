# T06 — Wiring: `kubernetes.yaml` + `variables-qa.yaml`

**Status**: **DONE (authored + syntax-checked + parse-verified, uncommitted in silmarils)** — 2026-05-15
**Repo**: `takamulai/silmarils` — edit `iac/eng-infra/shared-k8s/ansible/kubernetes.yaml` and `iac/eng-infra/shared-k8s/ansible/variables-qa.yaml`
**Blocks**: T08
**Blocked by**: T05 (closed)

## Reason

15a + 15b are no-ops until `kubernetes.yaml` actually calls them, and
the renders are no-ops until `variables-qa.yaml` carries the
`silmarils.apisix.*` block — including the 17 `cn_whitelist` and
17 `org_routing` entries. This task is the contract between the
plumbing (T03–T05) and the real-world inputs (T01 G8).

## Input

- Current `iac/eng-infra/shared-k8s/ansible/kubernetes.yaml`
  (per F02: the "placeholder" is a 3-line comment banner at lines
  198-200; **insert** two `include_tasks` blocks into the gap, do not
  delete the banner).
- Existing `variables-qa.yaml` for silmarils.* naming convention and YAML
  style.
- **Assume-defaults mode (user 2026-05-15)** — G8 owner-input not
  available; use placeholder defaults documented below. Iterate later.
- All 17 CN→lfi-id mappings — transcribe verbatim from
  `apisix/apisix.yaml.template` lines 16-50, **including** the NBF/EIB
  swap flagged as F03 (do not silently "fix" it; that's an owner call).
- PLAN.md §Per-env variable schema.
- **F06 schema add**: `silmarils.apisix.client_certs.passphrase: "password"`
  (explicit; do not rely on the playbook's default).
- **T05 schema adds**: `silmarils.apisix.allowed_egress_pods` and
  `silmarils.apisix.allowed_egress_ip_blocks`. For QA defaults:
  ```yaml
  allowed_egress_pods:
    - { namespace: "silmarils-{{ silmarils_environment }}", app: jisr-simulator, port: 8080 }
    - { namespace: "silmarils-{{ silmarils_environment }}", app: mbridge-mock,   port: 8080 }
  allowed_egress_ip_blocks: []
  ```
- **org_routing defaults (placeholder until G8 reply)**: all 17
  lfi-ids point at the Jisr simulator:
  ```yaml
  org_routing:
    lfi-FAB: &org_default_sim
      host: jisr-simulator.silmarils-qa.svc.cluster.local
      port: 8080
      scheme: http
      path_prefix: /mbridge/ws
    lfi-ADCB: *org_default_sim
    lfi-ADIB: *org_default_sim
    # ... 14 more, all pointing to same sim ...
  ```
  (Or expand each entry explicitly without YAML anchors if the
  silmarils ansible convention prefers it — pattern-match what's
  already in `variables-qa.yaml`.)

## Output

### Edit `iac/eng-infra/shared-k8s/ansible/kubernetes.yaml`

Replace the placeholder lines (verify line numbers — PLAN.md says
199-200 but the file may have shifted) with two include_tasks blocks:

```yaml
- name: APISIX base (Layer 15a)
  ansible.builtin.include_tasks:
    file: playbooks/15a-apisix.yaml
  tags:
    - apisix-base
    - apisix
  when: silmarils.apisix.enabled | default(false)

- name: APISIX silmarils LFI overlay (Layer 15b)
  ansible.builtin.include_tasks:
    file: playbooks/15b-apisix-silmarils-lfi.yaml
  tags:
    - apisix-silmarils-lfi
    - apisix
  when: silmarils.apisix.enabled | default(false)
```

### Edit `iac/eng-infra/shared-k8s/ansible/variables-qa.yaml`

Add full `silmarils.apisix.*` block, matching PLAN.md schema:

```yaml
silmarils:
  apisix:
    enabled: true
    namespace: apisix-silmarils
    fqdn: silmarils-qa-apisix.takamul.cc
    image:
      repository: silmarilsacr.azurecr.io/silmarils-apisix
      tag: <tag from T02>          # reconcile with T02's actual CI tag
    replicas: 2
    resources:
      requests: { cpu: "200m", memory: "256Mi" }
      limits:   { cpu: "1",    memory: "512Mi" }
    autoscaling:
      enabled: true
      min_replicas: 2
      max_replicas: 4
      cpu_target: 70
    cn_whitelist:
      # 17 entries from apisix.yaml.template
      "system@fab.com":  "lfi-FAB"
      "system@adcb.com": "lfi-ADCB"
      # ... (15 more)
    org_routing:
      # 17 entries — values from T01 G8
      lfi-FAB:
        host: jisr-simulator.silmarils-qa.svc.cluster.local
        port: 8080
        scheme: http
        path_prefix: /mbridge/ws
      lfi-ADCB:
        # ...
    dc_tang:
      host: dc-tang.silmarils-qa.svc.cluster.local
      port: 28888
      scheme: https
    forward_auth:
      uri: https://dc-tang.silmarils-qa.svc.cluster.local:28888/auth/inbound
      ssl_verify: true
```

## Acceptance criteria

- `ansible-lint` clean on both files.
- `ansible-playbook kubernetes.yaml --tags apisix-base,apisix-silmarils-lfi -e silmarils_environment=qa --syntax-check` clean.
- First real apply produces the full set of K8s objects (see PLAN.md
  *Deploy flow*).
- Second real run (idempotency check) reports **zero changes**.
- All 17 CN→lfi-id entries cross-checked against
  `apisix/apisix.yaml.template` — no missing or extra entries.
- All 17 org_routing entries cross-checked against the T01 G8 lock.

## Notes

- Verify the kubernetes.yaml insertion point before editing — PLAN.md
  references "lines 199-200" but that file changes; locate by the
  surrounding context (probably between the previous numbered playbook
  call and either silmarils-specific or monitoring playbooks).
- The image tag value depends on T02's tag-emission decision; do not
  hard-code `3.11.0-silmarils.1` if CI emits `<sha>-suffixed` tags.
- The 17 entries are the only place hand-typed values live. Mistypes
  here cause silent 403 (cert-validator) or 503 (org-router) at
  request time — paste them from a single source and review once.
- `dev` is out of scope this PR; do not touch `variables-dev.yaml`.

## Execution log (2026-05-15)

- Authored by background agent under T06 dispatch; verified by Claude.
- **`kubernetes.yaml`**: banner at line 199 renamed
  `# 15 — Placeholder (removed)` → `# 15a-b — APISIX (MIT-5211)`;
  inserted 13 lines for two `block:` entries (15a + 15b), both gated
  `when: silmarils.apisix.enabled | default(false) | bool`, tagged
  `apisix-base` / `apisix-silmarils-lfi` plus the umbrella `apisix`
  tag. Indentation matches surrounding sections (e.g. `03-04`
  Security & Certificates).
- **`variables-qa.yaml`**: 144-line `apisix:` block inserted under
  `silmarils:`, positioned between `gateway_ui` and `connectors` (so
  stand-alone networked components stay grouped before the
  connector cluster — agent's call, matches deploy ordering).
  Includes: `enabled`, `namespace`, `fqdn`, `image`, `replicas`,
  `resources` (block style — matches file convention), `autoscaling`,
  `client_certs.passphrase: "password"` (F06),
  `allowed_egress_pods` (jisr-simulator + mbridge-mock per T05),
  `allowed_egress_ip_blocks: []`, **17 `cn_whitelist` entries
  verbatim** (F03 swap kept with inline `# F03` markers),
  **17 `org_routing` entries expanded explicitly** (no YAML anchors
  — agent confirmed `variables-qa.yaml` has zero anchors elsewhere
  so pattern-match no-anchors), `dc_tang`, `forward_auth`.
- `ansible-playbook --syntax-check ... -e silmarils_environment=qa`
  clean (standard "No inventory" warnings only).
- `python3 -c "import yaml; yaml.safe_load(...)"` parses cleanly.
- `--check` fails at `00-initialize-variables.yaml` asserting
  `platform_type is defined` — pre-existing env-prereq, **unrelated**
  to T06's wiring; same failure for any tag run without that variable.
- **F07 filed**: `image.tag: "main"` is a placeholder that won't
  resolve in ACR until T02 CI runs and emits a real tag. Two paths
  to unblock T08 documented in F07; recommendation is path 1 (manual
  tag bump after first CI run) to keep tags immutable for now.
- Status: **silmarils-side changes are uncommitted local edits**. Will
  land with the consolidated MIT-5211 silmarils PR.
