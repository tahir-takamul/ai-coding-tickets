# T01 — Pre-implementation gate verification

**Status**: pending
**Repo**: `takamulai/silmarils` (mostly verification — only file change is potential `.p12` relocation if G5 chooses that path); some operator-side cluster reads.
**Blocks**: T02, T03, T04, T05, T06, T07
**Blocked by**: —

## Reason

`PLAN.md` locks eight ready-to-implement gates (G1–G8) that must be true
before any 15a/15b file authoring begins. T02+ inputs (runtime UID, ACR
pull strategy, `.p12` source path, `cloudflared` SA name, locked
`org_routing` targets) are speculation until each gate is closed. Closing
the gates first means later Tnn specs don't need rework as discoveries
land.

## Input

- `PLAN.md` §Ready-to-implement gates.
- Current `STATE.md` gate table.
- Live `silmarils-qa` AKS context (`silmarils-aks-admin` kubeconfig) for
  G1, G3, G7.
- Local Docker for G2, G4.
- Silmarils platform / LFI owners for G8 sign-off on `org_routing`
  targets.

## Output

Per gate, an executable verification step plus the result captured in
`STATE.md`:

| Gate | Verification command / action | Captured where |
|---|---|---|
| G1 — AKS managed-identity has `AcrPull` on `silmarilsacr` | `az aks show -g <rg> -n <cluster> --query identityProfile.kubeletidentity` then `az role assignment list --assignee <kubelet-objectId> --scope <acr-resource-id>` | STATE.md gate row; if absent, file deviation + add ACR pull-secret sync as a T03 sub-step (mirror `07b-sync-secrets.yaml`). |
| G2 — Upstream `apache/apisix:3.11.0-debian` runtime UID | `docker run --rm --entrypoint id apache/apisix:3.11.0-debian` | STATE.md gate row; locked value drives `securityContext.runAsUser` / `runAsGroup` in T03's deployment template and the `USER` directive in T02's Dockerfile. |
| G3 — AKS ≥ 1.27 (PSA stable) | `kubectl version` against silmarils-qa | STATE.md gate row; if < 1.27, escalate — PSA-restricted is a plan-locked decision. |
| G4 — PSA-restricted compatibility of `apache/apisix:3.11.0-debian` | Apply a trial pod (in `apisix-silmarils-test` ns or similar throwaway): `runAsNonRoot: true`, `readOnlyRootFilesystem: true`, emptyDir at `/usr/local/apisix/logs`, subPath ConfigMap mounts for `conf/apisix.yaml` + `conf/org_routing.json`, drop ALL capabilities, seccomp RuntimeDefault. Confirm pod Ready and `apisix start` succeeds. | STATE.md gate row; trial pod manifest saved to `task/logs/T01-psa-trial.yaml`. |
| G5 — `.p12` relocation policy | Decide between (a) copy under `iac/eng-infra/shared-k8s/ansible/files/silmarils/apisix-client-certs/`, (b) symlink from there to `apisix/client-certs/`, or (c) `lookup('fileglob', ...)` reaching back into the repo root. Recommendation: (a) for ansible determinism; local dev keeps the originals. | STATE.md gate row; if (a), perform the copy (only file change in this task). |
| G6 — Cloudflare Tunnel sibling PR | File (= what T07 does) and confirm merge plan with eng-infra owner. | STATE.md gate row links to the PR. T07 is the actual work. |
| G7 — `cloudflared` SA name + namespace | `kubectl -n <cf-namespace> get deploy cloudflared -o jsonpath='{.spec.template.spec.serviceAccountName}'`; namespace from `kubectl get deploy -A | grep cloudflared`. | STATE.md gate row; locked values feed T05's NetworkPolicy `from.namespaceSelector`/`podSelector`. |
| G8 — QA `org_routing` upstream targets | Lock all 17 bank → upstream pairs with silmarils owners. Default hypothesis: in-cluster simulators (`jisr-simulator.silmarils-qa.svc.cluster.local:8080`, `mbridge-mock.silmarils-qa.svc.cluster.local:8080`, …). Decision required: which banks point at sims vs real upstreams for QA. | STATE.md gate row table holding the 17 entries; T06 reads from there. |

## Acceptance criteria

- STATE.md gate table shows all 8 rows as **green** or
  **deviation-documented** (with a one-line link to the deviation).
- No code edits in silmarils/ or eng-infra/ as part of this task, except
  the optional `.p12` relocation under G5.
- Trial-pod manifest from G4 archived under `task/logs/`.
- The 17 locked CN→lfi-id and lfi-id→upstream pairs are committed to
  STATE.md (or a dedicated `task/G8-org-routing.md`) so T06 has a
  single, authoritative source.

## Notes

- Gates G1, G3, G7 need someone with `silmarils-aks-admin` kubeconfig
  context — if I don't have it, escalate to operator with the exact
  command. Do not block T01 closure on access alone; record "operator
  verified on YYYY-MM-DD via …" instead.
- Flip STATE.md gate rows individually as each closes; don't batch.
- If a gate fails the way the plan assumes (e.g. G3 returns 1.26),
  STOP and re-open PLAN.md — that's a planning deviation, not a
  T01 problem to work around.
